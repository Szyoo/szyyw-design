/* ============================================================
   @szyyw/design · version.js
   运行时可读的包版本——package.json 不随 vendored 文件走，
   检测更新要靠这份。发版时与 package.json 同步改（见 README 升级流程）。
   ============================================================ */

export const VERSION = "0.6.1";

/** 上游仓库（owner/repo），检测更新与变更对比链接都指向它 */
export const REPO = "Szyoo/szyyw-design";
